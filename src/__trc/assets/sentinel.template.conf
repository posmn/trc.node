# specify path to terracoin.conf or leave blank
# default is the same as TerracoinCore
terracoin_conf={{{ROOT_DIR}}}/data/terracoin.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name={{{ROOT_DIR}}}/data/sentinel.db
db_driver=sqlite
